<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/ripple_color">

    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/fill_color"/>
            <corners android:radius="?attr/corner_radius"/>
            <stroke
                android:width="?attr/stroke_width"
                android:color="?attr/stroke_color"/>
            <padding
                android:left ="?attr/padding_h"
                android:right="?attr/padding_h"
                android:top   ="?attr/padding_v"
                android:bottom="?attr/padding_v"/>
        </shape>
    </item>

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="?attr/corner_radius"/>
            <solid android:color="@android:color/white"/>
        </shape>
    </item>
</ripple>
